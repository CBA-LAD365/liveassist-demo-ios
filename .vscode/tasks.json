{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "xcodebuild -workspace liveassist-demo.xcworkspace -scheme liveassist-demo -destination 'generic/platform=iOS'"
        },
        {
            "label": "analyze",
            "type": "shell",
            "command": "xcodebuild -workspace liveassist-demo.xcworkspace -scheme liveassist-demo -destination 'generic/platform=iOS' analyze"
        }
    ]
}